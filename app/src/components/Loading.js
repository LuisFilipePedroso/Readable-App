import React from 'react'
import Logo from '../images/Logo.svg'

const Loading = (props) => (
    <div className="loading-div">
        <img alt="" src={Logo} className="loading-logo" />
    </div>
)

export default Loading